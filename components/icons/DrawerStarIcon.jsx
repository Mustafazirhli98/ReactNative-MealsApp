import { Ionicons } from "@expo/vector-icons"

const DrawerStarIcon = ({ color, size }) => {
    return (
        <Ionicons name="star" color={color} size={size} />
    )
}

export default DrawerStarIcon